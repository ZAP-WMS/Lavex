import 'package:flutter/material.dart';
import 'package:lavex/widgets/custom_scaffold.dart';

class LedgerBalance extends StatelessWidget {
  const LedgerBalance({super.key});

  @override
  Widget build(BuildContext context) {
    return CommonScaffold(body: 
    Column(children: [
      Row(
        children: [],
      )
    ],));
  }
}